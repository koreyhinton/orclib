# NEW RESEARCH

Looking for a new pattern to reduce intermediate state.

## QUESTION

Is it possible to express clearly (expressed intention matches behavior), in a common programming language, a data structure that reads data from a single snapshot and can only write data once for the scoped lifetime in a structured programming block, with the number of logic (if-then) branching of the data unrestricted, but the special requirement both expressed and honored?

